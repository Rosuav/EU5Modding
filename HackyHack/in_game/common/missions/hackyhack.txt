hackyhack_mission_tree = {
	icon = generic_capital_economy 
	repeatable = yes
	player_playstyle = administrative
	visible = { game_has_missions_enabled = yes }
	chance = 3600

	# TODO: Record the money on hand at start of mission, and ask that you get 10% more
	on_start = { }
	on_completion = { }
	
	mission_make_money = {
		final = yes
		enabled = {
			gold = 1000
		}
		duration = 0
		on_completion = {
			add_gold = 100
		}
	}
}
